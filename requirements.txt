# Minimal CPU-only requirements for the LBRCE FastAPI RAG service
# This file is for the deployed API only, not ingestion or test scripts.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
python-dotenv>=1.0,<2.0

langgraph>=1.0,<2.0
openai>=2.0,<3.0
pinecone>=7.0,<8.0
tavily-python>=0.7,<1.0

numpy>=2.0,<3.0
sentence-transformers>=5.0,<6.0

# Install the CPU wheel instead of the CUDA/NVIDIA build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
